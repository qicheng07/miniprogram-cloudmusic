# 数据库初始化操作指南

## 一、前置准备

### 1.1 确认信息
- ✅ 小程序AppID: `wx1b17543f3213e4bf`
- ✅ 云环境ID: `cloud1-d0gg2y757ffdbfb46`
- ✅ 已安装微信开发者工具

### 1.2 打开云开发控制台
1. 打开微信开发者工具
2. 打开项目：`miniprogram-cloudmusic`
3. 点击工具栏上的"云开发"按钮
4. 进入云开发控制台

---

## 二、创建数据库集合

### 2.1 创建 `songs` 集合

#### 步骤1: 创建集合
1. 在云开发控制台，点击左侧"数据库"
2. 点击"添加集合"按钮
3. 输入集合名称：`songs`
4. 点击"确定"

#### 步骤2: 配置权限
1. 点击 `songs` 集合
2. 点击"权限设置"标签
3. 设置权限：
   - **读权限**: 选择"所有用户可读"
   - **写权限**: 选择"仅管理员可写"
4. 点击"保存"

#### 步骤3: 创建索引
1. 点击"索引管理"标签
2. 点击"添加索引"
3. 创建以下索引：

| 索引名称 | 索引字段 | 排序方式 | 是否唯一 |
|---------|---------|---------|---------|
| name_text | name | 升序(1) | 否 |
| singer_text | singer | 升序(1) | 否 |
| isHot_index | isHot | 升序(1) | 否 |
| playCount_index | playCount | 降序(-1) | 否 |

---

### 2.2 创建 `playlists` 集合

#### 步骤1: 创建集合
1. 点击"添加集合"按钮
2. 输入集合名称：`playlists`
3. 点击"确定"

#### 步骤2: 配置权限
1. 点击 `playlists` 集合
2. 点击"权限设置"标签
3. 设置权限：
   - **读权限**: 选择"所有用户可读"
   - **写权限**: 选择"仅创建者可写"
4. 点击"保存"

#### 步骤3: 创建索引
创建以下索引：

| 索引名称 | 索引字段 | 排序方式 | 是否唯一 |
|---------|---------|---------|---------|
| name_text | name | 升序(1) | 否 |
| userId_index | userId | 升序(1) | 否 |
| isPublic_index | isPublic | 升序(1) | 否 |

---

### 2.3 创建 `users` 集合

#### 步骤1: 创建集合
1. 点击"添加集合"按钮
2. 输入集合名称：`users`
3. 点击"确定"

#### 步骤2: 配置权限
1. 点击 `users` 集合
2. 点击"权限设置"标签
3. 设置权限：
   - **读权限**: 选择"仅创建者可读写"
   - **写权限**: 选择"仅创建者可读写"
4. 点击"保存"

#### 步骤3: 创建索引
创建以下索引：

| 索引名称 | 索引字段 | 排序方式 | 是否唯一 |
|---------|---------|---------|---------|
| _id_unique | _id | 升序(1) | 是 |

---

## 三、配置云存储

### 3.1 权限设置
1. 在云开发控制台，点击左侧"存储"
2. 点击"权限设置"标签
3. 设置权限为：
   ```json
   {
     "read": true,
     "write": true
   }
   ```

### 3.2 创建文件夹
在存储中创建以下文件夹：
- `avatars/` - 用于存放用户头像

---

## 四、导入初始数据

### 4.1 导入数据到 `songs` 集合

1. 在云开发控制台，点击 `songs` 集合
2. 点击"导入"按钮
3. 选择 `initial_songs_data.jsonl` 文件
4. 文件类型选择"JSON Lines"
5. 冲突处理选择"插入"
6. 点击"导入"

---

## 五、部署云函数

### 5.1 云函数列表（16个）

| 云函数名称 | 功能描述 | 状态 |
|-----------|---------|------|
| `getHotSongs` | 获取热门歌曲列表 | ✅ |
| `searchSongs` | 搜索歌曲 | ✅ |
| `getSongDetail` | 获取歌曲详情 | ✅ |
| `getPlaylists` | 获取歌单列表 | ✅ |
| `getPlaylistDetail` | 获取歌单详情 | ✅ |
| `createPlaylist` | 创建歌单 | ✅ |
| `updatePlaylist` | 更新歌单 | ✅ |
| `deletePlaylist` | 删除歌单 | ✅ |
| `addSongToPlaylist` | 添加歌曲到歌单 | ✅ |
| `removeSongFromPlaylist` | 从歌单移除歌曲 | ✅ |
| `getUserInfo` | 获取用户信息 | ✅ |
| `updateUserInfo` | 更新用户信息 | ✅ |
| `toggleFavorite` | 收藏/取消收藏歌曲 | ✅ |
| `getFavoriteSongs` | 获取收藏歌曲列表 | ✅ |
| `getImageUrl` | 获取云存储临时文件URL | ✅ |
| `uploadAvatar` | 上传用户头像到云存储 | ✅ |

### 5.2 部署步骤

1. 在微信开发者工具左侧文件目录中，找到 `cloudfunctions` 文件夹
2. 右键点击 `cloudfunctions` 文件夹，选择"同步云函数列表"
3. 对每个云函数文件夹执行以下操作：
   - 右键点击云函数文件夹
   - 选择"上传并部署：云端安装依赖"
   - 等待部署完成

---

## 六、通用技术要求实现清单

### 6.1 用户登录与身份识别 ✅
- [x] 调用 `wx.getUserProfile` 获取用户信息
- [x] 在云数据库的每条记录中关联用户身份（`_openid`）
- [x] 页面能显示"当前用户是谁"（首页显示昵称或头像）

### 6.2 云存储使用 ✅
- [x] 用户头像上传功能
- [x] 使用 `uploadAvatar` 云函数上传到云存储
- [x] 使用 `getImageUrl` 云函数获取临时URL

### 6.3 加载状态与错误处理 ✅
- [x] 数据加载时显示 loading 提示
- [x] 网络请求失败时有错误提示
- [x] 表单提交成功后有成功反馈（toast）

### 6.4 页面数量 ✅
- [x] 首页（发现页）
- [x] 搜索页
- [x] 播放页
- [x] 个人中心页
- [x] 歌单详情页
- [x] 创建歌单页

---

## 七、权限配置详解

### 7.1 songs 集合权限
```json
{
  "read": true,
  "write": false
}
```

### 7.2 playlists 集合权限
```json
{
  "read": true,
  "write": "doc.userId == auth.openid"
}
```

### 7.3 users 集合权限
```json
{
  "read": "doc._id == auth.openid",
  "write": "doc._id == auth.openid"
}
```

---

**操作指南版本**: v1.2  
**创建时间**: 2026-06-24  
**预计完成时间**: 30-45分钟