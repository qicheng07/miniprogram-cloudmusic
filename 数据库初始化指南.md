# 数据库初始化操作指南

## 一、前置准备

### 1.1 确认信息
- ✅ 小程序AppID: `wx1b17543f3213e4bf`
- ✅ 云环境ID: `cloud1-d0gg2y757ffdbfb46`
- ✅ 已安装微信开发者工具

### 1.2 打开云开发控制台
1. 打开微信开发者工具
2. 打开项目：`miniprogram-cloudmusic`
3. 点击工具栏上的"云开发"按钮
4. 进入云开发控制台

---

## 二、创建数据库集合

### 2.1 创建 `songs` 集合

#### 步骤1: 创建集合
1. 在云开发控制台，点击左侧"数据库"
2. 点击"添加集合"按钮
3. 输入集合名称：`songs`
4. 点击"确定"

#### 步骤2: 配置权限
1. 点击 `songs` 集合
2. 点击"权限设置"标签
3. 设置权限：
   - **读权限**: 选择"所有用户可读"
   - **写权限**: 选择"仅创建者可写"
4. 点击"保存"

#### 步骤3: 创建索引
1. 点击"索引管理"标签
2. 点击"添加索引"
3. 创建以下索引：

| 索引名称 | 索引字段 | 排序方式 | 是否唯一 |
|---------|---------|---------|---------|
| name_index | name | 升序(1) | 否 |
| singer_index | singer | 升序(1) | 否 |
| isHot_index | isHot | 升序(1) | 否 |
| playCount_index | playCount | 降序(-1) | 否 |

**索引创建步骤**:
- 索引名称：`name_index`
- 索引字段：点击"添加字段"，选择 `name`，排序方式选择"升序(1)"
- 点击"确定"
- 重复以上步骤创建其他索引

---

### 2.2 创建 `playlists` 集合

#### 步骤1: 创建集合
1. 点击"添加集合"按钮
2. 输入集合名称：`playlists`
3. 点击"确定"

#### 步骤2: 配置权限
1. 点击 `playlists` 集合
2. 点击"权限设置"标签
3. 设置权限：
   - **读权限**: 选择"所有用户可读"
   - **写权限**: 选择"仅创建者可写"
4. 点击"保存"

#### 步骤3: 创建索引
创建以下索引：

| 索引名称 | 索引字段 | 排序方式 | 是否唯一 |
|---------|---------|---------|---------|
| name_index | name | 升序(1) | 否 |
| userId_index | userId | 升序(1) | 否 |
| isPublic_index | isPublic | 升序(1) | 否 |

---

### 2.3 创建 `users` 集合

#### 步骤1: 创建集合
1. 点击"添加集合"按钮
2. 输入集合名称：`users`
3. 点击"确定"

#### 步骤2: 配置权限
1. 点击 `users` 集合
2. 点击"权限设置"标签
3. 设置权限：
   - **读权限**: 选择"仅创建者可读写"
   - **写权限**: 选择"仅创建者可读写"
4. 点击"保存"

#### 步骤3: 创建索引
创建以下索引：

| 索引名称 | 索引字段 | 排序方式 | 是否唯一 |
|---------|---------|---------|---------|
| _id_index | _id | 升序(1) | 是 |

---

## 三、导入初始数据

### 3.1 准备数据文件

我已经为您准备了初始歌曲数据，保存在 `initial_songs_data.jsonl` 文件中。

**⚠️ 重要提示**: 云数据库导入要求 **JSON Lines格式**（每行一个JSON对象），不是JSON数组格式。

**JSON Lines格式示例**（正确）:
```
{"name": "晴天", "singer": "周杰伦", "album": "叶惠美", ...}
{"name": "夜曲", "singer": "周杰伦", "album": "十一月的萧邦", ...}
{"name": "稻香", "singer": "周杰伦", "album": "魔杰座", ...}
```

**JSON数组格式**（错误，会导致导入失败）:
```json
[
  {"name": "晴天", "singer": "周杰伦", ...},
  {"name": "夜曲", "singer": "周杰伦", ...}
]
```

### 3.2 导入数据到 `songs` 集合

#### 方法1: 使用控制台导入（推荐）
1. 在云开发控制台，点击 `songs` 集合
2. 点击"导入"按钮
3. 选择 `initial_songs_data.jsonl` 文件（注意是.jsonl后缀）
4. 文件类型选择"JSON Lines"或"JSON"
5. 冲突处理选择"插入"
6. 点击"导入"

#### 方法2: 手动添加（测试用）
1. 点击 `songs` 集合
2. 点击"添加记录"
3. 逐条添加歌曲数据

### 3.3 验证数据导入
1. 点击 `songs` 集合
2. 查看记录数量（应该有41条记录）
3. 随机查看几条记录，确认数据格式正确

### 3.4 使用真实曲库数据（推荐）

当前提供的测试数据仅包含41首歌曲，如需更丰富的曲库，可以使用开源音乐API获取真实数据：

**推荐的开源音乐API**:
1. **NeteaseCloudMusicApi** - 网易云音乐API
   - GitHub: https://github.com/Binaryify/NeteaseCloudMusicApi
   - 可获取歌曲信息、歌词、封面等

2. **music-api** - 多平台音乐API
   - 支持网易云、QQ音乐、酷狗等平台
   - GitHub: https://github.com/jsososo/music-api

3. **UnblockNeteaseMusic** - 网易云音乐解锁
   - 可获取音乐播放链接
   - GitHub: https://github.com/nondanee/UnblockNeteaseMusic

**使用建议**:
- 部署上述API到自己的服务器
- 通过云函数调用API获取歌曲数据
- 将数据存入云数据库songs集合

---

## 四、部署云函数

### 4.1 部署所有云函数

#### 步骤1: 打开云函数目录
在微信开发者工具左侧文件目录中，找到 `cloudfunctions` 文件夹

#### 步骤2: 批量部署
1. 右键点击 `cloudfunctions` 文件夹
2. 选择"同步云函数列表"
3. 等待同步完成

#### 步骤3: 逐个部署云函数
对每个云函数文件夹执行以下操作：

1. 右键点击云函数文件夹（如 `getHotSongs`）
2. 选择"上传并部署：云端安装依赖"
3. 等待部署完成（看到"上传成功"提示）
4. 重复此步骤部署所有云函数

**需要部署的云函数列表**:
- ✅ getHotSongs
- ✅ searchSongs
- ✅ getSongDetail
- ✅ getPlaylists
- ✅ getPlaylistDetail
- ✅ createPlaylist
- ✅ getUserInfo
- ✅ toggleFavorite
- ✅ getFavoriteSongs
- ⏳ updatePlaylist（待开发）
- ⏳ deletePlaylist（待开发）
- ⏳ addSongToPlaylist（待开发）
- ⏳ removeSongFromPlaylist（待开发）
- ⏳ updateUserInfo（待开发）

### 4.2 验证云函数部署

#### 方法1: 控制台测试
1. 在云开发控制台，点击"云函数"
2. 点击已部署的云函数名称
3. 点击"测试"标签
4. 输入测试参数，点击"运行测试"
5. 查看返回结果

**测试示例**:
```json
{
  "limit": 10,
  "offset": 0
}
```

#### 方法2: 小程序调用测试
1. 编译运行小程序
2. 进入首页，查看是否显示热门歌曲
3. 进入搜索页，测试搜索功能
4. 进入个人中心，查看用户信息

---

## 五、权限配置详解

### 5.1 权限规则说明

#### songs集合权限
```json
{
  "read": true,
  "write": "doc._openid == auth.openid"
}
```
- **读权限**: `true` 表示所有人可读
- **写权限**: 只有创建者（_openid等于当前用户openid）可写

#### playlists集合权限
```json
{
  "read": true,
  "write": "doc.userId == auth.openid"
}
```
- **读权限**: 所有人可读（公开歌单）
- **写权限**: 只有歌单创建者可写

#### users集合权限
```json
{
  "read": "doc._id == auth.openid",
  "write": "doc._id == auth.openid"
}
```
- **读权限**: 只有本人可读
- **写权限**: 只有本人可写

### 5.2 权限配置注意事项
1. ⚠️ 权限配置错误会导致数据泄露或无法访问
2. ⚠️ 建议先用宽松权限测试，再逐步收紧
3. ⚠️ 敏感操作必须通过云函数完成

---

## 六、索引配置详解

### 6.1 索引作用
- 提高查询速度
- 支持排序操作
- 减少数据库扫描

### 6.2 索引配置建议
1. **常用查询字段**: 为经常查询的字段创建索引
2. **排序字段**: 为需要排序的字段创建索引
3. **组合查询**: 为多字段组合查询创建复合索引

### 6.3 索引注意事项
1. ⚠️ 索引会占用存储空间
2. ⚠️ 写入数据时需要更新索引，会影响写入性能
3. ⚠️ 不要创建过多索引，按需创建

---

## 七、常见问题排查

### 问题1: 云函数调用失败
**现象**: 小程序调用云函数返回错误
**排查步骤**:
1. 检查云函数是否已部署
2. 检查云函数代码是否有语法错误
3. 检查云函数日志（云开发控制台 -> 云函数 -> 日志）
4. 检查数据库集合是否存在

### 问题2: 数据库查询返回空
**现象**: 云函数执行成功但返回空数组
**排查步骤**:
1. 检查数据库集合是否有数据
2. 检查查询条件是否正确
3. 检查索引是否创建
4. 检查权限配置

### 问题3: 权限错误
**现象**: 提示"权限不足"
**排查步骤**:
1. 检查数据库权限配置
2. 检查用户是否已登录
3. 检查云函数中的权限验证逻辑

---

## 八、完成检查清单

### 数据库创建检查
- [ ] 已创建 `songs` 集合
- [ ] 已创建 `playlists` 集合
- [ ] 已创建 `users` 集合
- [ ] 已配置所有集合权限
- [ ] 已创建所有必要索引
- [ ] 已导入初始歌曲数据（至少20条）

### 云函数部署检查
- [ ] 已部署 `getHotSongs` 云函数
- [ ] 已部署 `searchSongs` 云函数
- [ ] 已部署 `getPlaylists` 云函数
- [ ] 已部署 `createPlaylist` 云函数
- [ ] 已部署 `getUserInfo` 云函数
- [ ] 已部署 `toggleFavorite` 云函数
- [ ] 已部署 `getFavoriteSongs` 云函数
- [ ] 已测试所有云函数功能

### 功能测试检查
- [ ] 首页能显示热门歌曲
- [ ] 搜索功能正常
- [ ] 能创建歌单
- [ ] 能收藏歌曲
- [ ] 个人中心显示正常

---

## 九、下一步操作

完成以上所有步骤后，请：
1. 在微信开发者工具中编译运行小程序
2. 测试所有功能是否正常
3. 如有问题，查看云函数日志进行排查
4. 确认无误后，通知我继续完善缺失的云函数

---

**操作指南版本**: v1.0
**创建时间**: 2026-06-17
**预计完成时间**: 30-45分钟
